import { Propio } from "../Componentes/propio";

export function Principal(){
     return (
        <div className="flex flex-col items-center justify-center bg-gray-200 min-h-full w-full">
            <Propio />
        </div>
    );
}